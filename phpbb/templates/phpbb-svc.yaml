apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" .Release.Name "phpbb" | trunc 24 }}
  labels:
    app: phpbb
    provider: phpbb-server
    release: {{ .Release.Name }}
    heritage: bitnami
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https
  selector:
    app: phpbb
    provider: phpbb-server
    release: {{ .Release.Name }}
