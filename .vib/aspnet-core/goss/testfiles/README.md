# How to update the example application to deploy

* Run the exampleApp.sh script and update the runtime-parameters.yaml file with the generated secret

```bash
cd .vib/aspnet-core/goss/testfiles
./exampleApp.sh
```
