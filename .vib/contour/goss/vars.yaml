contour:
  serviceAccount:
    automountServiceAccountToken: true
  podSecurityContext:
    fsGroup: 1002
  containerSecurityContext:
    runAsUser: 1002
  service:
    ports:
      xds: 8001
envoy:
  service:
    ports:
      http: 80
