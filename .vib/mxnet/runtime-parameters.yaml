entrypoint:
  workDir: /app
mode: standalone
persistence:
  mountPath: /bitnami/mxnet
standalone:
  podSecurityContext:
    enabled: true
    fsGroup: 1002
  containerSecurityContext:
    enabled: true
    runAsUser: 1002
