master:
  extraFlags:
    - --maxmemory-policy allkeys-lru
  persistence:
    enabled: false
replicas:
  extraFlags:
    - --maxmemory-policy allkeys-lru
  persistence:
    enabled: false
auth:
  usePassword: false
