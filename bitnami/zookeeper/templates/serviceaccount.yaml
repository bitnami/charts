{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "zookeeper.fullname" .) .Values.serviceAccount.name }}
  labels: {{- include "zookeeper.labels" . | nindent 4 }}
    role: zookeeper
{{- end }}
