apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metallb.fullname" . }}-test-connection"
  labels: {{- include "metallb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: bitnami/minideb:buster
      command: ['wget']
      args:  ['{{ include "metallb.fullname" . }}:7472']
  restartPolicy: Never
