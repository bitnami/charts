{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and ( include "opensearch.ingest.enabled" . ) .Values.ingest.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-metrics" (include "opensearch.ingest.fullname" .) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: opensearch
    app.kubernetes.io/component: ingest
  {{- $defaultAnnotations := dict "prometheus.io/scrape" "true" "prometheus.io/port" (.Values.ingest.metrics.service.port | quote) "prometheus.io/path" "/_prometheus/metrics" }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list $defaultAnnotations .Values.ingest.metrics.service.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
spec:
  type: {{ .Values.ingest.metrics.service.type }}
  {{- if and .Values.ingest.metrics.service.clusterIP (eq .Values.ingest.metrics.service.type "ClusterIP") }}
  clusterIP: {{ .Values.ingest.metrics.service.clusterIP }}
  {{- end }}
  {{- if (or (eq .Values.ingest.metrics.service.type "LoadBalancer") (eq .Values.ingest.metrics.service.type "NodePort")) }}
  externalTrafficPolicy: {{ .Values.ingest.metrics.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if eq .Values.ingest.metrics.service.type "LoadBalancer" }}
  loadBalancerSourceRanges: {{ .Values.ingest.metrics.service.loadBalancerSourceRanges }}
  {{- end }}
  {{- if (and (eq .Values.ingest.metrics.service.type "LoadBalancer") (not (empty .Values.ingest.metrics.service.loadBalancerIP))) }}
  loadBalancerIP: {{ .Values.ingest.metrics.service.loadBalancerIP }}
  {{- end }}
  {{- if .Values.ingest.metrics.service.sessionAffinity }}
  sessionAffinity: {{ .Values.ingest.metrics.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.ingest.metrics.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.ingest.metrics.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  ports:
    - name:  http-metrics
      targetPort: rest-api
      port: {{ .Values.ingest.metrics.service.ports.metrics }}
      protocol: TCP
      {{- if (and (or (eq .Values.ingest.metrics.service.type "NodePort") (eq .Values.ingest.metrics.service.type "LoadBalancer")) (not (empty (coalesce .Values.ingest.metrics.service.nodePort .Values.ingest.metrics.service.nodePorts.metrics)))) }}
      nodePort: {{ .Values.ingest.metrics.service.nodePorts.metrics }}
      {{- else if eq .Values.ingest.metrics.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.ingest.podLabels .Values.commonLabels ) "context" . ) }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: ingest
{{- end }}
