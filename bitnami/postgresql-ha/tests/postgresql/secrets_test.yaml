suite: test secret
templates:
  - postgresql/secrets.yaml
tests:
  - it: should be empty if postgresql.existingSecret is specified
    set:
      postgresql.existingSecret: my-secret
    asserts:
      - hasDocuments:
          count: 0
