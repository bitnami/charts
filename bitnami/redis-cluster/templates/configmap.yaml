apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "redis-cluster.fullname" . }}
  labels: {{- include "redis-cluster.labels" . | nindent 4 }}
data:
  redis.conf: |-
    dir {{ .Values.persistence.path }}
    port {{ .Values.redisPort }}
    cluster-enabled yes
    cluster-config-file /bitnami/nodes.conf
    appendonly yes
{{- if .Values.configmap }}
{{- include "redis-cluster.tplValue" (dict "value" .Values.configmap "context" $) | nindent 4 }}
{{- end }}
