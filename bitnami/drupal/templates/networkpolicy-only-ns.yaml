{{- if and .Values.networkPolicy.enabled .Values.networkPolicy.ingress.allowOnlyFromNamespace }}
apiVersion: {{ include "common.capabilities.networkPolicy.apiVersion" . }}
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-from-ns" (include "common.names.fullname" .) }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
spec:
  podSelector:
    matchLabels:
      {{- include "common.labels.standard" . | nindent 6 }}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          {{- if .Values.networkPolicy.ingress.allowOnlyFromNamespaceNameLabel }}
          {{- include "common.tplvalues.render" (dict "value" .Values.networkPolicy.ingress.allowOnlyFromNamespaceNameLabel "context" $) | nindent 10 }}
          {{- end }}
{{- end }}
