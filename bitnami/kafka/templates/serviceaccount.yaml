{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "kafka.fullname" .) .Values.serviceAccount.name }}
  labels: {{- include "kafka.labels" . | nindent 4 }}
    role: kafka
{{- end }}
