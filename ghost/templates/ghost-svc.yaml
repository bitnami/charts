apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" .Release.Name "ghost" | trunc 24 }}
  labels:
    app: ghost
    provider: ghost-server
    release: {{ .Release.Name }}
    heritage: bitnami
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: ghost
    provider: ghost-server
    release: {{ .Release.Name }}
