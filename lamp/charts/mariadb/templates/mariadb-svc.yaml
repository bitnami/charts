apiVersion: v1
kind: Service
metadata:
  name: {{template "mariadb.fullname" .}}
  labels:
    provider: mariadb
    heritage: bitnami
spec:
  ports:
  - name: mysql
    port: 3306
    targetPort: mysql
  selector:
    app: {{template "mariadb.fullname" .}}
