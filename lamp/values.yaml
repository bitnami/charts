# Default values for lamp.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value
mariadb:
  mariadbDatabase: lamp-app
  mariadbUser: lamp-user
  mariadbPassword: lamp-secret
