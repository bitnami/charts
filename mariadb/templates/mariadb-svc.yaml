apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}
  labels:
    provider: mariadb
    release: {{ .Release.Name }}
    heritage: bitnami
spec:
  ports:
  - name: mysql
    port: 3306
    targetPort: mysql
  selector:
    provider: mariadb
    release: {{ .Release.Name }}
