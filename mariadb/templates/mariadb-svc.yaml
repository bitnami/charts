apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}
  labels:
    provider: {{ .Chart.Name }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - name: mysql
    port: 3306
    targetPort: mysql
  selector:
    provider: {{ .Chart.Name }}
    release: {{ .Release.Name }}
