apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  labels:
    app: rabbitmq
    provider: rabbitmq-server
    heritage: bitnami
spec:
  type: LoadBalancer
  ports:
  - name: rabbitmq
    port: 15672
    targetPort: rabbitmq
  selector:
    app: rabbitmq
    provider: rabbitmq-server
