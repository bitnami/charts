apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" .Release.Name "nginx" | trunc 24 }}
  labels:
    provider: nginx
    release: {{ .Release.Name }}
    heritage: bitnami
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https
  selector:
    provider: nginx
    release: {{ .Release.Name }}
