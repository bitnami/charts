apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" .Release.Name "joomla" | trunc 24 }}
  labels:
    app: joomla
    provider: joomla-server
    release: {{ .Release.Name }}
    heritage: bitnami
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https
  selector:
    app: joomla
    provider: joomla-server
    release: {{ .Release.Name }}
