apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" .Release.Name "odoo" | trunc 24 }}
  labels:
    app: odoo
    provider: odoo-server
    release: {{ .Release.Name }}
    heritage: bitnami
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: odoo
    provider: odoo-server
    release: {{ .Release.Name }}
