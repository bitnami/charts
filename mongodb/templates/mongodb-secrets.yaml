apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-%s" .Release.Name "mongodb" | trunc 24 }}
  labels:
    provider: mongodb
    release: {{ .Release.Name }}
    heritage: bitnami
type: Opaque
data:
  mongodb-root-password: {{ .Values.mongodbRootPassword | quote | b64enc | quote }}
  mongodb-password: {{ .Values.mongodbPassword | quote | b64enc | quote }}
